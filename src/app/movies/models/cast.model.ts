export class Cast {
    constructor(public id: string, public movieId: number, public iMDB: string, public character: string, public firstName: string, public lastName: string, public age: number, public birthPlace: string){
    }
}
